name: General Assignment
# run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4 # загружаем реп. на виртуальную машину
  finish:  
    runs-on: ubuntu-latest
    needs: build
    steps:
    - run: echo "Проект успешно выгружен"
